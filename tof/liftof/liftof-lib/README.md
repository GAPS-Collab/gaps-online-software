# liftof-lib - common functions for both RB and server code

This library contains functions which are useful for both 
codebases, the readoutboard (liftof-rb) as well as the
server side (liftof-cc, liftof-tui).

